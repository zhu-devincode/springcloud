server:
  port: 3344
  
spring:
  application:
    name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhu-devincode/microservicecloud-config.git #git仓库名 
      
